main.jsx > keep as it is
index.css > root and css variables
App.jsx > routes, wrapped with AuthProvider
HomePage.jsx > for default landing page
ShoppingPage.jsx > default products lists
ShopPage.jsx > default shop list
Layout.jsx > for UI holder, render Outlets Routes

improve the CSS and CSS classnames in jsx files
target is mobile view always
you just improve .jsx and css files for UI only