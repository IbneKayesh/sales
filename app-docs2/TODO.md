# TODO

- [ ] Review requirements (CSS rewrite + compact full-screen UI; nested CRUD hierarchy with done/undone tasks).
- [ ] Inventory existing implementation:
  - [ ] `src/pages/*` (Features, Tables, Task/Sidebar pages)
  - [ ] `src/components/*` (shared UI components: modal/confirm/empty/loading/toasts)
  - [ ] `src/hooks/*` (project/module/submodule/feature/table/task CRUD)
  - [ ] `server/*` routes
- [ ] Inspect current CSS (`src/index.css`, `src/App.css`) and layout structure (`App.jsx`, `TopBar.jsx`).
- [ ] Identify gaps vs requirements:
  - [ ] Projects list with add module button per project
  - [ ] Modules expandable rows with sidebar CRUD + tasks (done/undone + remove)
  - [ ] Submodules expandable rows with sidebar CRUD + tasks
  - [ ] Features expandable rows with sidebar CRUD + tasks and table dropdown per feature
  - [ ] Tables list/grid with sidebar CRUD + columns sidebar CRUD
- [ ] Draft edit plan (file-level) for CSS + UI hierarchy changes.
- [ ] Implement CSS rewrite:
  - [ ] small UI components
  - [ ] white smoke + gray primary colors
  - [ ] full-screen layout with reduced margins/padding
- [ ] Implement UI hierarchy:
  - [ ] Projects table/grid + sidebar CRUD
  - [ ] Nested expand/collapse for modules/submodules/features and the correct “add” buttons
  - [ ] Sidebars at each level for edit/delete
  - [ ] Task list under relevant entities with done/undone toggle + remove
- [ ] Implement Tables + Columns management:
  - [ ] tables list/grid with sidebar CRUD
  - [ ] “Columns” sidebar (add/edit/delete columns)
  - [ ] per-feature dropdown to manage related tables + sidebar CRUD
- [ ] Validate integration:
  - [ ] Frontend build passes
  - [ ] Server starts
  - [ ] API communication works for all CRUD and task toggles
- [ ] Remove dead/unused code and ensure no unresolved TODOs remain.

