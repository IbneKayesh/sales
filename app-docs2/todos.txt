You are a senior full-stack engineer.

This project is a React frontend with an Express backend inside a `/server` folder.

Your task is to COMPLETE the project by reading ALL existing files, comments, TODOs, placeholder functions, and requirement notes.

Rules:
1. Carefully inspect the entire codebase before making changes.
2. Treat comments as implementation specifications.
3. Fully implement all missing logic.
4. If a required file, folder, utility, route, hook, component, or service is missing:
   - CREATE it automatically.
   - Add proper imports/exports.
5. Do NOT leave placeholders, TODOs, mock implementations, or incomplete functions.
6. Ensure frontend and backend integrate correctly.
7. Preserve the existing architecture and coding style.
8. Use modern best practices.
9. Avoid unnecessary rewrites unless required.
10. Fix broken imports, type issues, dependency issues, and runtime errors.
11. Add environment variable support where necessary.
12. Create reusable utilities instead of duplicating logic.
13. Validate all API requests and handle errors properly.
14. Add loading states and error states in the frontend where appropriate.
15. Ensure responsive UI if UI components exist.
16. If database integration exists partially:
   - complete models
   - queries
   - validation
17. If comments describe features:
   - implement them fully, not partially.
18. If tests exist:
   - make them pass.
19. If scripts or configs are missing:
   - create them.

Important:
- Never ask for confirmation.
- Never stop at analysis.
- Directly implement the project.
- Continue until the app is fully runnable.

After implementation:
1. Verify imports.
2. Verify build passes.
3. Verify server starts.
4. Verify client starts.
5. Verify API communication works.
6. Remove dead code.
7. Ensure no unresolved TODOs remain.

Output requirements:
- Show all created files.
- Show all modified files.
- Briefly explain major implementations.
- Mention any assumptions made.

Tech stack assumptions:
- Frontend: React
- Backend: Express (inside `/server`)
- Use the existing stack already present in package.json files.
- Prefer consistency with current dependencies before adding new ones.

Priority:
1. Functionality
2. Maintainability
3. Clean architecture
4. UX
5. Performance


phase 2:
rewrite css:
- all ui components will be small
- white smoke and gray  is primary color
- full screen will be use with less margin and padding

features:
- a table with full width, contains list of projects on click show a sidebar thats allow edit, delete the project.
and add module button with each project line, on click show a sidebar thats allow edit, delete the modules. add remove done undone task.

- expand modules row, contains list of modules under the project, on click show a sidebar thats allow edit, delete the module.
and add sub module button with each modules line, on click show a sidebar thats allow edit, delete the sub modules. add remove done undone task.

- expand sub modules row, contains list of sub modules under the module, on click show a sidebar thats allow edit, delete the sub module.
and add feature button with each sub modules line, on click show a sidebar thats allow edit, delete the features. add remove done undone task.

- expand feature row, contains list of feature under the sub module, on click show a sidebar thats allow edit, delete the sub module
and dropdown of tables with each features line, on click show a sidebar thats allow edit, delete the features and tables. add remove done undone task.


tables:
- a table/grid with full width, contains list of tables on click show a sidebar thats allow edit, delete the table.
and add columns button with each table line, on click show a sidebar thats allow edit, delete the columns.

