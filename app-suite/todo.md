read below files need to implement them

phase 1:
[] pages/M01/ThemePage.jsx / Layout / Component Size / set range 50 to 150, default 100
[] pages/M01/ThemePage.jsx / Layout / Corner / set range 0 to 50, default 6
[] pages/M01/ThemePage.jsx / Layout / Density / set range 50 to 150, default 75, ensure scale density must be work properly with UI
[] pages/M01/ThemePage.jsx / Layout / Boxed Gap / set range 10 to 50, default 15, ensure left and right space must be as set value
[] pages/M01/ThemePage.jsx / Wallpaper presets / when select Topbar/Workspace/ Page Background / Page Title then selected image make checked, already Workspace show selectecd image

phase 2:
[x] POS UI > src/pages/M02/pos/*.jsx (3 styles: TwoPanel, FullGrid, Terminal)

phase 3:
[] all *.jsx files, find inline CSS, make sure all common to App.css and index.css, if already in App.css or index.css, replace inline css to class name

phase 4: (improvements)
[]

ensure no extra new css, until really its require