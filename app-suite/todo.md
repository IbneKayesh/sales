read below files need to implement them

[] when user click a button(1) show a small popup with file uploader, user will select a file, show file as preview, user will click upload button(2), upload successfully, and next time when user click button(1) show preview uploaded file, when again user will click on preview show option download/delete/replace, select download will download file, delete will show Confirm Component for user dicision, replace will allow select file again and user click button(2) for upload file, use property for max file and file size
[] a new file upload component need to create, use modal component as parent
[] update ExamplePage with new File Upload Component


don't write extra css